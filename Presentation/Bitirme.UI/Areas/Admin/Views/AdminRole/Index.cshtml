@model List<Bitirme.Domain.Entities.Roles>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

<a class="btn btn-primary" href="/Admin/AdminRole/Create">Role Ekle</a>
<br />
<br />
<table class="table table-bordered" id="table">
    <tr>
        <th>Id</th>
        <th>Rolü</th>

        <th>Sil</th>
        <th>Güncelleme</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>

          @*  @{
                string adminUrl = Url.Action("Delete", "AdminRole", new { area = "Admin", id = item.Id });
            }
            <td><a href="@adminUrl" class="btn btn-danger" onclick="return confirm('silmek istiyormsunuz?')">Sil</a></td>
*@
            <td class="btn ">@Html.ActionLink("Sil","Delete",new {item.Id,area = "Admin"},new {@class="btn btn-danger",onclick="return confirm ('silmek istiyormsunuz?')"})</td>
            @*<td><a class="btn btn-danger" onclick="return confirm ('silmek istiyormsunuz?')" href="/Admin/AdminRole/Delete/@item.Id">Sil</a></td>*@
            <td><a class="btn btn-primary" href="/Admin/AdminRole/Update/@item.Id">Güncelleme</a></td>

        </tr>
    }

</table>
<a class="btn btn-info" href="/Admin/AdminRole/UserRoleList">Kullanıcı Rol Listesi</a>



